# Comma-separated list of origins allowed to call this API (your Vercel URL + localhost for dev)
ALLOWED_ORIGINS=http://localhost:5173,https://your-app.vercel.app

# Set automatically by Render when you attach a Postgres instance.
# Leave unset locally to use a local SQLite file instead.
# DATABASE_URL=postgresql://user:password@host:5432/dbname
